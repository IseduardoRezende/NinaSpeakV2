@using NinaSpeakV2.Domain.Entities
@model IEnumerable<BaseError>

@if (Model is not null)
{
    foreach (var error in Model)
    {
        <ul>
            <li class="text-danger">@error.Description</li>
        </ul>
    }
}
